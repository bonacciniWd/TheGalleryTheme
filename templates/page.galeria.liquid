{% layout none %}

<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Galeria | thegallery.co</title>
  <style>
    body {
      margin: 0;
      background-color: #000;
      color: #fff;
      font-family: 'Georgia', serif;
    }
    .hero {
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 0 1.5rem;
    }
    .hero h1 {
      font-size: 3rem;
      letter-spacing: 0.05em;
      margin-bottom: 1rem;
    }
    .hero p {
      max-width: 600px;
      color: #aaa;
      font-size: 1.2rem;
    }
    .gallery {
      background-color: #111;
      padding: 6rem 2rem;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 3rem;
    }
    .artwork {
      position: relative;
      background-color: #222;
      border-radius: 1rem;
      overflow: hidden;
      box-shadow: 0 0 30px rgba(255,255,255,0.05);
    }
    .artwork img {
      width: 100%;
      height: auto;
      transition: transform 0.4s ease;
    }
    .artwork:hover img {
      transform: scale(1.05);
    }
    .caption {
      text-align: center;
      margin-top: 1rem;
    }
    .caption h3 {
      font-size: 1.2rem;
      margin-bottom: 0.25rem;
    }
    .caption p {
      font-size: 0.875rem;
      color: #888;
    }
    footer {
      text-align: center;
      color: #555;
      padding: 4rem 1rem;
    }
  </style>
</head>
<body>
  <section class="hero">
    <h1>Arte que transcende paredes</h1>
    <p>Explore uma curadoria exclusiva de obras que flutuam entre o físico e o digital. Uma experiência imersiva da TheGallery.co.</p>
  </section>

  <section class="gallery">
    {% for i in (1..4) %}
      <div class="artwork">
        <img src="https://source.unsplash.com/600x800?art,portrait,{{ i }}" alt="Obra {{ i }}">
        <div class="caption">
          <h3>Título da Obra {{ i }}</h3>
          <p>Artista Convidado</p>
        </div>
      </div>
    {% endfor %}
  </section>

  <footer>
    © 2025 thegallery.co — Todos os direitos reservados
  </footer>
</body>
</html>
